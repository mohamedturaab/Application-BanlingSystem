package ApplicationReveser;import java.util.Stack;public class Reverse {    public String reverse(String input){        Stack<Character> std = new Stack<>();        for (int i = 0; i<input.length();i++){            std.push(input.charAt(i));        }        String  reverser = "";        while (!std.empty()){            reverser+= std.pop();        }        return reverser;    }//    public String reverse(String input) {//////        Stack<Character> std = new Stack<Character>();////        for (int i = 0; i < input.length(); i++) {////            std.push(input.charAt(i));////////           }////        String reversed = "";////        while (!std.empty()) {////            reversed += std.pop();////////////        }//////            StringBuilder reversed = new StringBuilder("");////            while (!std.empty()) {////                reversed.append(std.pop());////            }////////            return reversed.toString();////        }////////    }   }